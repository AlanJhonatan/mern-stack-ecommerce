import React from 'react';

// import Card from './Card';
// import Search from './Search';

// import { getProducts } from './apiCore';

const Checkout = ({ products }) => {
  return <p>products: {JSON.stringify(products)}</p>;
};

export default Checkout;
